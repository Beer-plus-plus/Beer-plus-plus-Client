import React from 'react';

const Logo = () => {
  return (
    <div className="logo" style={{ width: '100%', margin: '50px auto 0 auto' }}>
      <img src="Beer++logo.svg" alt="loading" style={{ width: '100%' }}></img>
    </div>
  );
};

export default Logo;
